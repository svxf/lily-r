#pragma once

#include "../classes/classes.hpp"

#include <cstdint>

struct globals
{
	static cat::roblox::instance_t datamodel;
	static cat::roblox::instance_t players;
	static cat::roblox::instance_t localplayer;
};